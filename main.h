#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>

/* Print character */
char _putchar(char c);

/* Print string */
int _puts(char *s);

/* Return length of string */
int _strlen(char *s);

/* Print number */
int print_number(int n);

/* customized printf */
int _printf(const char *format, ...);

#endif /* MAIN_H */
